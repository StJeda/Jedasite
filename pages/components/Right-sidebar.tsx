import { FC } from "react";
import sass from '/styles/mainpage.module.scss'

const RightSidebar: FC = () => {
    return(
        <div className={sass.right_sidebar}>
        </div>
    )
}

export default RightSidebar;
